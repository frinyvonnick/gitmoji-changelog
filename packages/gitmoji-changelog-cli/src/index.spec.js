import { main } from './index'

describe('cli', () => {
  it('should throw an error if not a git project', () => {
    main()
  })
  it('should create a changelog file if not exist', () => {

  })
  it('should update a changelog file if exists', () => {

  })
})
